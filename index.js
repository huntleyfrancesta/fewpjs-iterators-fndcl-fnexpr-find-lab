const testVar = {}

function testFunc() {
    return "hi"
}
superbowlWin = (array) => {
    let results = array.find(elem => elem.result === 'W');
    if (result) {
        return result.year;
    } else {
        return underfined;
    }
}